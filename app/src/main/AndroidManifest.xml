<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permisos deINTERNET -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permisos Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:maxSdkVersion="30" />

    <!-- Características opcionales -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">

        <!-- Activity de Login (LAUNCHER) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity Home -->
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Activity Ingreso Vehículo -->
        <activity
            android:name=".IngresoVehiculoActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Activity Salida Vehículo -->
        <activity
            android:name=".SalidaVehiculoActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Activity Mantenimiento -->
        <activity
            android:name=".MantenimientoActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Activity del escáner QR de ZXing -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            tools:replace="screenOrientation" />


        <!-- Activity Confirmación de Salida -->
        <activity
            android:name=".SalidaConfirmacionActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Activity Gestión de Usuarios -->
        <activity
            android:name=".GestionUsuariosActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
    </application>

</manifest>